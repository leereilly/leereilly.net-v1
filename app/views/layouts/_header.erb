<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
<title><%= @title %></title>
<%= javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jquery/1.3.2/jquery.min.js' %>
<%= javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jqueryui/1.5.2/jquery-ui.min.js' %>
<%= javascript_include_tag 'jquery.textEffects.js' %>
<%= javascript_include_tag 'jquery.bubbleup.js' %>
<%= javascript_include_tag 'jquery.prettyPhoto.js' %>
<%= javascript_include_tag 'jquery.konamiCode.js' %>

<%= stylesheet_link_tag('style', :media => "all") %>
<%= stylesheet_link_tag('prettyPhoto', :media => "all") %>

<script type="text/javascript">
  $(function(){
    $("ul#menu li img").bubbleup({tooltips: true});
  });
  
   $(document).ready(function() {

     $("#about-link").click(function(){
        $("#about").effect( "pulsate", 5,
         300 );
     });

  });
  
  
   $(document).ready(function() {

     $("#contact-link").click(function(){
        $("#menu").effect( "pulsate", 5, 
         300 );  
     });

  });
  
	$(document).ready(function(){
		$(window).konami(function(){
			$.prettyPhoto.open('http://www.youtube.com/watch?v=GfO-0f8cjwk','NERD ALERT','My PSN and XBOX Live handle is <strong>leereillyDOTnet</strong> - come get some!');
		});
	});
  
  jQuery(document).ready(function($) {

  $("a[rel^='prettyPhoto']").prettyPhoto({

  });
  });  
  
</script>

<script type="text/javascript">
  if (document.all || document.getElementById) {
    var thetitle=document.title
    document.title=''
  }

  var data="GCAT";
  var done=1;

  function statusIn(text) {
    decrypt(text,20,10);
  }

  function statusOut() {
    self.status='';
    done=1;
  }

  function decrypt(text, max, delay) {
	  if (done) {
		  done = 0;
		  decrypt_helper(text, max, delay,  0, max);
	  }
  }

  function decrypt_helper(text, runs_left, delay, charvar, max) {
	  if (!done) {
	    runs_left = runs_left - 1;
	    var status = text.substring(0,charvar);
	    for (var current_char = charvar; current_char < text.length; current_char++) {
		    status += data.charAt(Math.round(Math.random()*data.length));
		  }
	    document.title = status;
	    var rerun = "decrypt_helper('" + text + "'," + runs_left + "," + delay + "," + charvar + "," + max + ");"
	    var new_char = charvar + 1;
	    var next_char = "decrypt_helper('" + text + "'," + max + "," + delay + "," + new_char + "," + max + ");"
	    if (runs_left > 0) {
		    setTimeout(rerun, delay);
		  }
	    else {
		    if (charvar < text.length) {
			    setTimeout(next_char, Math.round(delay*(charvar+3)/(charvar+1)));
			  }
		    else {
			    done = 1;
			  }
		  }
	  }
  }
  
  if (document.all||document.getElementById) {
    statusIn(thetitle);
  }
</script>

</head>
<body>
<div xmlns:v="http://rdf.data-vocabulary.org/#" typeof="v:Person">
<!--  <a href="http://github.com/leereilly">
    <img style="position: absolute; top: 0; right: 0; border: 0;" src="http://s3.amazonaws.com/github/ribbons/forkme_right_gray_6d6d6d.png" alt="Fork me on GitHub" />
  </a>
-->
<div id="wrapper">
  <div id="header">
    <div id="logo">
      <a href="http://www.leereilly.net" rel="v:url"><h1 id="title"><span property="v:name"><%= @title %></span></h1></a>
      <h2 id="description"><span property="v:title">Software Developer</span></h2>
   </div>

  <div id="topright">
    <ul>
      <li><a href="#" id="about-link">about</a></li>
      <li><a href="#" id="contact-link">contact</a></li>
    </ul>
  </div>
  <div class="cleared"></div>
</div>
<!-- Closes header -->
<div class="lee-pulse">
</div>
